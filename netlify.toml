[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Force use of newer Ubuntu Jammy build image
[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--version"
